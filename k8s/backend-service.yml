apiVersion: v1
kind: Service
metadata:
  name: hft-backend
  namespace: solana-hft
  labels:
    app: hft-backend
spec:
  selector:
    app: hft-backend
  ports:
  - name: http
    port: 3000
    targetPort: 3000
  type: ClusterIP